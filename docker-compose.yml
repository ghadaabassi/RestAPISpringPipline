services:
  spring-boot-app:
    image: backend
    ports:
      - "0.0.0.0:8091:8091"
    environment:
      - SPRING_DATABASE_URL=jdbc:mysql://mysql:3306/api_etudiant
      - SPRING_DATASOURCE_USERNAME=ghada
      - SPRING_DATASOURCE_PASSWORD=1234
      - SPRING_DATASOURCE_DRIVER_CLASS_NAME=com.mysql.cj.jdbc.Driver
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
    depends_on:
      - mysql

  mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: "api_etudiant"
      MYSQL_ROOT_PASSWORD: "1234"
      MYSQL_USER: "ghada"
      MYSQL_PASSWORD: "1234"
    ports:
      - "0.0.0.0:3306:3306"
    volumes:
      - api_etudiant:/var/lib/mysql

volumes:
  api_etudiant:
